---
id: production-mawster-no-use
title: Production MAWster (pre-move copy — DO NOT USE)
type: reference
status: hidden
order: 9999
nav: hidden
revised: 2026-08
summary: Pre-move copy. Canonical tree is mawizorek/uritp-docs → production-mawster/.
---

# 🪦 Pre-move copy — DO NOT USE

**Canonical home: `mawizorek/uritp-docs` → `production-mawster/`.**

Michael moved the MAWster tree to `uritp-docs` on 2026-08-08 so it renders on a known path, renaming this folder `-no-use` in the same commit (`105ab87`). Everything here is a **snapshot from before that move** and is stale by definition.

⚠️ **Second-claimant warning.** Every file in here has a live counterpart in `uritp-docs`. A snapshot that still reads as authoritative is exactly how three retired manifests went wrong: it resolves, it looks right, and it is out of date. **Read the `uritp-docs` copy, always.**

🚫 **Not deleted on purpose.** Michael named this folder and the cull is his call, not an agent's. If it is dead, it gets flagged per the Deletion-Flag Gate and he removes it.

# Smith Theatre — layers — moved

🗑️ **Tombstone. Ready to delete.**

The 29-row design layer list has moved to the URITP documentation, where a page was already scaffolded for it. See [venues/README.md](../README.md).

A layer list is an inventory of one building. The *convention* behind it — that the layer says where a thing is and carries its elevation band, never a class — is portable and lives in [standards/vectorworks](../../standards/vectorworks/).

/* ParamGetText ( json ; key ; default )
   Text value when the key is present, otherwise default.
   Present-but-empty returns empty, not the default. */

Case (
    ParamHas ( json ; key ) ;
    JSONGetElement ( json ; key ) ;
    default
)

/* ParamGetText ( json ; key ; fallback )
   Text value when the key is present, otherwise fallback.
   Present-but-empty returns empty, not the fallback. */

Case (
    ParamHas ( json ; key ) ;
    JSONGetElement ( json ; key ) ;
    fallback
)

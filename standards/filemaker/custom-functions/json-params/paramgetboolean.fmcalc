/* ParamGetBool ( json ; key ; fallback )
   1 or 0 when the key is present, otherwise the coerced fallback. */

Case (
    ParamHas ( json ; key ) ;
    GetAsNumber ( JSONGetElement ( json ; key ) ) ≠ 0 ;
    GetAsNumber ( fallback ) ≠ 0
)